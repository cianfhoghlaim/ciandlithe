"""dlt_sources.law._context.eu — EU-context legal data (statute).

Lower-priority context for the BI jurisdiction pipelines per the v2
plan §A "context data" rules.

The ``eur_lex/`` subtree carries the 5 EUR-Lex statute modules
(``cjeu_case_law``, ``decisions``, ``directives``, ``regulations``,
``treaties``) that moved from cianfhoghlaim in
``2026-08-25-ciandlithe-context-carveout-v1``.

The English-Irish alignment subset (``eur_lex_celvic``,
``publications_office``) stays in cianfhoghlaim as BI-context per the
v2 plan §A.
"""
