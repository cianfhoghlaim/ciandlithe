# CIANDLITHE wholesale-copy of cianfhoghlaim/cianfhoghlaim @ main branch.
# Original: cianfhoghlaim/cianfhoghlaim (per the openspec/changes/ciandlithe-repo-bootstrap-v2/specs/ciandlithe-bootstrap-v2/spec.md).
# Migrated to ciandlithe: 2026-08-23
# Licence: BUSL-1.1 (per LICENSE.md)
#
# =============================================================================
# LITELLM — Pangolin EE root blueprint (private resource)
# =============================================================================
# Generated from the canonical Cianfhoghlaim 6-file stack contract.
# See: https://docs.pangolin.net/manage/blueprints
# Roles contain RBAC role names only; middleware references live in
# pangolin.yaml (the Traefik overlay).

private-resources:
  litellm:
    name: LiteLLM Gateway
    mode: http
    sites:
      - bunchloch
    destination: litellm
    destination-port: 4000
    full-domain: litellm.ciandlithe.ie
    ssl: true
    scheme: https
    enabled: true
    roles:
      - Member